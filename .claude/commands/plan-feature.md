Plan a new feature for this project before any code is written, so I can review the plan, suggest changes, and only start development once I'm confident it matches what I want.

The feature I want to plan is: **$ARGUMENTS**

Use the **feature-planning** skill to run this. Follow it strictly — in particular **the gate**: investigate the codebase, write the plan to `docs/plans/`, and iterate with me on it, but do **not** write any feature code until I explicitly approve the plan.

If `$ARGUMENTS` is empty, ask me what feature I want to plan before starting.
